/**
 * Re-export StandardCell from cells directory
 * This maintains backward compatibility while the new structure is in place
 */
export { StandardCell } from '@interface/components/EditableTable/cells/StandardCell';
